name "galatynarymby"

hud_fastswitch 	"1"

bind "kp_minus" 	"secammo;primammo;defuser;vesthelm"

// NUMPAD
// slash, asterisk, minus, plus
bind "*"			"hegren;flash;flash;smoke"
bind "kp_plus"		"primammo"
bind "kp_ins"		"secammo"

// 7 8 9
bind "kp_home" 		"hegren"
bind "kp_uparrow" 	"flash"
bind "kp_pgup" 		"sgren"

// 4 5 6
bind "kp_leftarrow" "deagle;secammo"
bind "kp_5" "awp;primammo"
bind "kp_rightarrow" "sg550;g3sg1;primammo"

// 1 2 3
bind "kp_end"		"ak47;m4a1;primammo"
bind "kp_downarrow" "galil;famas;primammo"
bind "kp_pgdn" 		"mp5;primammo"

// 0, del
bind "kp_ins"		"scout;primammo"
bind "kp_del"		"vest;vesthelm;defuser"

// mouse
sensitivity 	"2.5"
bind 			"mouse4"	"+jump"
bind 			"mouse5"	"weapon_knife;wait;lastinv"